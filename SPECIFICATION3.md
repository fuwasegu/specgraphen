# 仕様抽出エンジン 将来構想（specgraphen 研究ロードマップ）

## 位置づけ

`SPECIFICATION2.md` のコア・アルゴリズム（MDL／論理圧縮による決定表の抽出）は
実装完了済み（`specgraphen-logic` の Quine-McCluskey ＋ Espresso 系キューブ最小化、
`specgraphen-lift` の決定表抽出、MCP ツール `extract_core_rules`）。

本書は、そこから先の **探索的な（=まだ実装していない）構想**を仕様として書き起こす。
いずれも「人間が定めた構造（パッケージ境界・命名・コメント）に頼らず、
コードそのものの統計的・幾何学的な性質からドメインの真の構造を復元する」という
一貫した狙いを持つ。本書の記述は設計意図であり、確定した実装契約ではない。

> 注：扱う対象は一般のレガシーシステム（Java を主、SQL・画面テンプレートを従）。
> 具体的な解析対象や案件に依存する記述は本書には一切含めない。例示はすべて
> `com.example` 系の架空シンボルで行う。

---

## 0. 共通の前提：HG Space を基盤とする

両構想とも、新たにコードを再解析するのではなく、**lift 済みの Higher Graphen Space
（エンティティ ＝ セル、関係 ＝ インシデンス、各々に witness と confidence）**を
入力とする。これにより：

- すでに型解決・関係抽出・確信度計算を経たグラフの上で幾何・統計を回せる
- witness（file:line）を保持したまま結果を提示でき、既存ツール群と同じ
  「根拠つき・確信度つき」の出力規律を守れる
- 解析が不完全な領域（未解決参照など）は確信度の低いインシデンスとして
  自然に扱え、結果に「ここは確からしさが低い」と添えられる

両構想は新規クレート（例：`specgraphen-topology`）として、純粋な数理処理層を
I/O から独立させて実装することを想定する（`specgraphen-logic` と同じ方針）。

---

## 1. 構想A：トポロジカルデータ解析（TDA）と「真のドメイン境界」

### 1.1 動機

レガシーシステムのパッケージ構成は、しばしば組織変更・命名規約の揺れ・
歴史的経緯によって、実際の機能凝集とずれている。「人間が引いた線」ではなく
「コードの依存・データアクセスの近さが自然に作る塊」を見つけたい。

### 1.2 距離の定義

エンティティ（クラス／メソッド）を点とし、点間の距離をインシデンスから定義する。
候補となる近さの指標（複数を重み付き合成する想定）：

- **呼び出し近接度**：呼び出しグラフ上の最短距離 / 双方向の呼び出し頻度
- **データ共有度**：同一テーブル・同一カラム群へアクセスする度合い
  （`crud_matrix` / `column_usage` の出力を再利用）
- **型結合度**：共有する型・例外・アノテーションの重なり
- **共変更の痕跡**：同一 witness ファイルに同居する度合い（Git 履歴が無い前提なので、
  ファイル同居や近接行を代理指標とする）

これらから対称な距離行列、あるいは点群を構成する。

### 1.3 パーシステント・ホモロジー

距離（または類似度のしきい値）を連続的に動かしながら単体複体を成長させ
（Vietoris–Rips フィルトレーション等）、各しきい値で現れる連結成分・ループ・
空洞の「生成と消滅」をパーシステンス図として得る。

- **長く持続する連結成分（H0）＝ 安定したドメインの塊。**
  パッケージ境界と一致しない長寿命クラスタが「真のドメイン境界」の候補。
- **持続するループ（H1）＝ 循環的な依存構造**（双方向結合・god object 周辺の
  リング状依存など）の幾何学的シグナル。

### 1.4 出力イメージ

- 検出されたクラスタ一覧（構成エンティティ・代表 witness・persistence の長さ＝確信度）
- 各クラスタと既存パッケージ境界とのズレ（「このクラスタは3パッケージにまたがる」等）
- 移行時の「まとめて切り出す候補」「分割すべき god object」の提示
- MCP ツール案：`domain_clusters`（しきい値や指標の重みを引数に取り、
  クラスタと既存パッケージのズレを返す）

### 1.5 段階的実装

1. 距離行列の構成（既存クエリ出力の再利用、純粋関数）
2. 単純な階層クラスタリングで「それっぽい塊」を出す MVP（TDA 抜き）
3. パーシステント・ホモロジー導入、persistence を確信度に写像
4. MCP 公開・既存パッケージとのズレレポート

### 1.6 リスク・限界

- 距離指標の設計が結果を支配する。最初は単一指標＋可視化で妥当性を検証する。
- 大規模グラフでの計算量（Rips 複体は点数に対して急増）。
  クラスタ候補を粗く絞ってから局所的に TDA を回す段階化が要る。
- 「数学的に出た境界」が業務的に妥当とは限らない。あくまで人間の判断を補助する
  仮説として、根拠つきで提示する（断定しない）。

---

## 2. 構想B：コード層序学（Stratigraphy）— コードの「地層」年代測定

### 2.1 動機

Git 履歴の無いレガシーコードでも、コード片には「いつ頃書かれたか」の痕跡が
残っている。古いイディオムと新しいイディオムの混在パターンから、
**基盤となる初期仕様（最古層）と、後年のパッチ（堆積層）を分離**したい。
`SPECIFICATION2.md` の MDL がパッチ「ノイズ」を論理的に削るのに対し、
本構想はパッチを**年代で層別**する。

### 2.2 「同位体」候補（相対年代の手がかり）

放射性同位体に見立てる、時代と相関する観測可能な特徴：

- **言語イディオムの世代**：旧来の生コレクション／手書きループ vs ジェネリクス・
  拡張 for・ラムダ・Stream・var など、言語機能の導入時期に対応する書き方
- **API／ライブラリ世代**：非推奨 API の使用、世代の異なる同種ライブラリの併用、
  日時・IO・文字列処理の旧式／新式
- **規約の地層**：命名規約・例外処理スタイル・ロギング方法の不連続な切り替わり
- **コメント・整形の様式**：定型ヘッダコメントの様式差（内容ではなく形式のみを見る）

いずれも **特徴の存在パターン**のみを使い、コメント本文や識別子の意味内容は
年代づけの根拠にしない（＝対象固有情報に依存しない）。

### 2.3 相対年代づけ

絶対年代（西暦）は出さない。出すのは**相対的な層序**：

- 各特徴に「世代スコア」（その特徴が一般化した時期の順序）を与える
- エンティティ／ファイル単位で特徴の分布を集計し、最古層〜最新層に順序づけ
- 1つのメソッド内に複数世代が混在する場合、「古い骨格に新しいパッチが
  上書きされた」境界の候補としてマークする

### 2.4 出力イメージ

- ファイル／クラスの相対年代マップ（最古層＝初期仕様の核の候補）
- 「新しい層に厚く覆われた古い核」＝ リスクの高いリファクタ対象の提示
- メソッド内の世代混在点（パッチ境界の候補、witness つき）
- MCP ツール案：`code_strata`（相対層序と、最古層＝仕様の核の候補を返す）

### 2.5 段階的実装

1. 特徴検出器を tree-sitter パターンで実装（言語イディオム世代から）
2. 世代スコア表（順序のみ、外部知識として保持）＋エンティティ単位の集計
3. 相対層序の算出と可視化
4. MDL 抽出（`extract_core_rules`）との連携：「最古層に属する条件＝真の仕様」
   「最新層の条件＝後付けパッチ」という仮説を決定表の dead variable 解釈に重ねる

### 2.6 リスク・限界

- 「新しい書き方」が必ずしも「新しく書かれた」を意味しない（一括リフォーマット・
  自動変換で世代がならされることがある）。相関であって因果でないと明示する。
- 世代スコア表は言語・エコシステムへの外部知識依存。Java 以外へは要再設計。
- 絶対年代は主張しない。あくまで相対順序と「層の不連続」の検出にとどめる。

---

## 3. 全構想に共通する設計原則

既存の specgraphen の規律をそのまま継承する：

1. **根拠つき**：すべての結論に witness（file:line）を添える。
2. **確信度つき**：persistence の長さ・特徴の数などを confidence に写像し、
   「弱い仮説」と「強い仮説」を区別して提示する。
3. **不完全さに優雅に縮退**：解析が届かない領域は「不明」として正直に出す
   （`unknowns` と同じ思想）。完全性を装わない。
4. **断定しない**：数理が出した境界・年代は人間の判断を補助する仮説であり、
   最終決定ではないことを出力に明記する。
5. **純粋層の分離**：数理処理は I/O・AST から独立した新規クレートに置き、
   将来の汎用ライブラリ化・他言語展開を容易にする。

---

## 4. 現実性評価と優先度

| 構想 | 価値 | 難度 | 推奨着手順 |
|---|---|---|---|
| A: TDA／ドメイン境界 | 高（移行の切り出し単位に直結） | 中〜高 | まず階層クラスタリング MVP で距離指標を検証 → TDA |
| B: コード層序学 | 中〜高（初期仕様の核の特定） | 中 | 言語イディオム世代の検出器から小さく始める |

どちらも「まず単純版で距離指標／世代特徴の妥当性を実データで確かめ、
有効性が見えてから数理を重ねる」段階化が肝。`SPECIFICATION2.md` の MDL が
そうだったように、純粋な数理コアを独立クレートとして切り出してから、
lift／MCP に結線する順序を踏む。
