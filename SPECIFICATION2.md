# レガシーシステム仕様抽出エンジン（specgraphen-logic）開発構想

## プロジェクトの背景と目的
Gitのコミット履歴が参照できず、ソースコード一式（ZIPファイルなど）のみが提供されるレガシーシステムのフルリプレース案件において、「真の仕様」と「度重なるパッチによる技術的負債（ノイズ）」を決定的に分離する。
既存のコード意味抽出基盤 `specgraphen` と、セマンティック検索基盤 `kiri` のエコシステムを拡張し、自然科学や数理モデル（特に情報理論）を用いたアプローチで仕様をリバースエンジニアリングする。

## コア・アルゴリズム：MDL（最小記述長）と論理圧縮
条件分岐の複雑な連鎖から、MDL（Minimum Description Length）の原理に基づいて不要な条件（Don't care）を削ぎ落とし、極限まで圧縮された決定表（ディシジョンテーブル）を生成する。

* **シンボリック実行によるパス抽出**: エントリポイントから構文木（AST）を辿り、`if` や `switch` の分岐パスを列挙する。
* **カルノー図的平坦化**: 各パスの入力条件をブール代数として抽象化し、結果（出力やDB操作）が同じになるパターンをグルーピングする。
* **論理最適化（Reduction）**: Quine-McCluskey法や二分決定図（BDD）などのアルゴリズムを適用し、結果に一切影響を与えていない無駄な条件（死んだ変数や不要なパッチ）を数学的に特定・除外する。

## アーキテクチャ設計（Cargo Workspace構成）
既存の `specgraphen` リポジトリのCargo Workspace構成を活かし、純粋な数理ロジック層を完全に独立したクレートとして実装する。これにより、将来的な汎用ライブラリ化や `crates.io` への公開を容易にする。

**ディレクトリ構成のイメージ:**
`specgraphen/`
`├── Cargo.toml (Workspace root)`
`├── crates/`
`│   ├── specgraphen-cli/      (既存: CLIエントリ)`
`│   ├── specgraphen-mcp/      (既存: MCPサーバー)`
`│   ├── specgraphen-lift/     (既存: tree-sitter等での抽出)`
`│   ├── specgraphen-model/    (既存: ドメイン型)`
`│   └── specgraphen-logic/    (★新規: 論理圧縮・カルノー図エンジン)`

## 開発フェーズ（Claude Codeへの指示ステップ）

**フェーズ1：数理エンジンの純粋実装（specgraphen-logic）**
* `specgraphen-logic` クレートを新規作成する。
* ファイルI/OやASTの概念を一切含めず、純粋なブール代数の配列を受け取り、論理最適化（不要な条件の除外）を行って圧縮された真理値表を返すロジックを実装する。
* 複数のテストケース（Don't care が含まれるケース、すべて独立しているケースなど）に対するピュアなユニットテストを完備する。

**フェーズ2：抽出基盤との結合（specgraphen-lift）**
* `specgraphen-lift` の機能を拡張し、tree-sitterで解析したASTから、条件分岐の条件式を `specgraphen-logic` が解釈できるブール変数の構造体に変換・マッピングする処理を実装する。
* `specgraphen-model` の Space/Cell の概念と論理式の対応づけを行う。

**フェーズ3：MCPツールとしての公開（specgraphen-mcp）**
* AIエージェント（Claude等）が直接叩ける新しいMCPツール（例: `extract_core_rules`）を追加する。
* 指定したエントリポイントやクラスのパスを受け取り、平坦化・圧縮済みのクリーンなディシジョンテーブルをMarkdown等の構造化テキストとしてAIに返却する。

## 将来的な拡張アイデア（参考）
* **トポロジカルデータ解析（TDA）とパーシステントホモロジー**: 変数の依存関係やDB操作の近さから空間距離を定義し、パーシステントホモロジーを用いて人間が定めたパッケージ境界ではない「真のドメイン境界」を特定する。
* **コード層序学（Stratigraphy）**: 古い言語イディオムやライブラリのバージョンを放射性同位体に見立て、コードの「地層」を年代測定することで、基盤となる初期仕様を特定する。
